name: Publish to NPM

on:
  push:
    branches:
      - main

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      id-token: write # This is required for requesting the OIDC token
      contents: read # This is required for actions/checkout
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18.x'
          registry-url: 'https://registry.npmjs.org'
      - name: Install dependencies
        run: npm install
      - name: Run tests
        run: npm test
      # npm publish here uses GitHub OIDC trusted publishing, which only works
      # on GitHub Actions. On Forgejo this job still runs install + tests.
      - name: Publish to NPM
        if: github.server_url == 'https://github.com'
        run: npm publish